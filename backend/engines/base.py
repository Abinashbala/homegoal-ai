"""
HomeGoal AI — Base Engine Interface
"""
from abc import ABC

class BaseEngine(ABC):
    """
    Base class for all business logic engines.
    Engines must be stateless and testable.
    """
    pass
